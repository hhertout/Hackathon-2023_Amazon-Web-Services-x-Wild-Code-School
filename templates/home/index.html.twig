{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block content %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="container bg-dark">

		This friendly message is coming from:
		<ul>
			<li>Your controller at
				<code>
					<a href="{{ '/Users/hugueshertout/Desktop/hackathon/src/Controller/HomeController.php'|file_link(0) }}">src/Controller/HomeController.php</a>
				</code>
			</li>
			<li>Your template at
				<code>
					<a href="{{ '/Users/hugueshertout/Desktop/hackathon/templates/home/index.html.twig'|file_link(0) }}">templates/home/index.html.twig</a>
				</code>
			</li>
		</ul>
	</div>

	{{ form_start(searchForm) }}
	<button id="search">Search</button>
	{{ form_end(searchForm) }}
    <a class="btn" href="{{ path('app_home') }}">Clear Filter</a>

	<ul>
		{% if vehicles is not empty %}
			{% for vehicle in vehicles %}
				<li>{{ vehicle.brand }}
					{{vehicle.energy}}
					{{vehicle.company.name}}</li>
			{% endfor %}
		{% else %}
			<p class="text-light">No vehicle found</p>
		{% endif %}
	</ul>
{% endblock %}
