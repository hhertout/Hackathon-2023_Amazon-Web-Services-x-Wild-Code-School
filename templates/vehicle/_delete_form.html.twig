<form method="post" action="{{ path('app_vehicle_delete', {'company':vehicle.company.id ,'vehicle': vehicle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vehicle.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
