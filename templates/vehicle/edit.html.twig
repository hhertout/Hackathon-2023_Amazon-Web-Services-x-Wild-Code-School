{% extends 'base.html.twig' %}

{% block title %}Edit Vehicle
{% endblock %}

{% block content %}
	<div class="container">
		<h1 class="text-center m-4">Edit Vehicle</h1>
		<div class="row">
			<div class="col-sm-8 m-auto">
				<div class="card text-dark p-3 mb-5">
					{{ include('vehicle/_form.html.twig', {'button_label': 'Update'}) }}

					<a href="{{ path('app_company_home', {'company': vehicle.company.id}) }}" class="btn btn-lg btn-outline-dark mt-4">back to list</a>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
